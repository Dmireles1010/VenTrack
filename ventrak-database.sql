CREATE TABLE ITEM(
    ID INT NOT NULL,
    NAME VARCHAR(20),
    PRODUCTION_COMPANY VARCHAR(30),
    QUANTITY INT NOT NULL
);

CREATE TABLE VENDING_MACHINE(
    VENDING_MACHINE_ID INT NOT NULL,
    LOCATION VARCHAR(30),
    AMOUNT_OF_ITEMS INT,
    TOTAL_MONEY INT
);

INSERT INTO ITEM(ID, NAME, PRODUCTION_COMPANY, QUANTITY) VALUES(1, 'Chocolate Bar', 'Hersheys', 4);
